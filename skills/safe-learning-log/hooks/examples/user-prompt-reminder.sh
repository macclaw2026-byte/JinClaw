#!/bin/bash
set -e
cat << 'EOF'
<safe-learning-log-reminder>
If this task creates a durable workflow improvement, high-impact markdown change, hook installation, or new skill, produce a change report in .learnings/reports/ and tell the user what changed.
If a third-party skill may be installed, run skill-security-audit first.
If the task has meaningful risk of false confidence or missed signals, use the shared monitoring-and-retrofit framework to name the primary monitor, backstop, and blind spots.
</safe-learning-log-reminder>
EOF
